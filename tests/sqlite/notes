To make a in ram database, open the database file as ":memory:".  A NULL or empty string will case it to use a temporary file.

To have the database created if it doesn't exist, set the SQLITE_OPEN_CREATE flag.
